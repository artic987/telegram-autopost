name: Telegram Autopost

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: telegram-autopost
  cancel-in-progress: false

jobs:
  hold:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: No-op
        run: echo "Duplicate pending run replaced; no Telegram posts from this run."
